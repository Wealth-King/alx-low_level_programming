#include "main.h"

/**
 * _isupper = CHecl if the letter is upper
 * @x: the number to be checked
 * Returm: 1 for upper letter or 0 for amy else 
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
