#include <stdio.h>

int main() {
    // Using the puts function to print the message
    puts("Programming is like building a multilingual puzzle");

    // Ending the program with the value 0
    return 0;
}
